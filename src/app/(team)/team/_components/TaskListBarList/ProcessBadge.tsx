import { processBadgeWrapperStyle } from '@/app/(team)/team/_components/TaskListBarList/styles';

const ProcessBadge = () => {
  return <div className={`${processBadgeWrapperStyle}`}></div>;
};

export default ProcessBadge;
